name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact: LabClient-Windows
          - os: ubuntu-latest
            artifact: LabClient-Linux
          - os: macos-latest
            artifact: LabClient-macOS

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install flask flask-cors waitress pdfplumber requests playwright beautifulsoup4 lxml python-docx PyMuPDF Pillow selenium webdriver-manager openpyxl xlrd rapidocr-onnxruntime

      - name: Install Playwright browsers
        run: |
          pip install playwright
          playwright install chromium --with-deps

      - name: Build with PyInstaller (Windows)
        if: runner.os == 'Windows'
        working-directory: ./client
        run: |
          pyinstaller --clean lab_client.spec

      - name: Build with PyInstaller (Linux/macOS)
        if: runner.os != 'Windows'
        working-directory: ./client
        run: |
          pyinstaller --clean lab_client.spec

      - name: Create package (Windows)
        if: runner.os == 'Windows'
        working-directory: ./client
        run: |
          mkdir ..\release
          copy dist\lab-client.exe ..\release\lab-windows.exe

      - name: Create package (Linux)
        if: runner.os == 'Linux'
        working-directory: ./client
        run: |
          mkdir -p ../release
          cp dist/lab-client ../release/lab-linux

      - name: Create package (macOS)
        if: runner.os == 'macOS'
        working-directory: ./client
        run: |
          mkdir -p ../release
          cp dist/lab-client ../release/lab-macos

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: release/*

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: LabClient-Windows
          path: release-windows

      - name: Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: LabClient-Linux
          path: release-linux

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: LabClient-macOS
          path: release-macos

      - name: List files
        run: |
          echo "=== Windows ===" && ls -la release-windows/
          echo "=== Linux ===" && ls -la release-linux/
          echo "=== macOS ===" && ls -la release-macos/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            release-windows/lab-windows.exe
            release-linux/lab-linux
            release-macos/lab-macos